// tipe variable

// Box A = biru
// Box B = merah

// Box C = kosong

// Box A -> pindah isi ke box c
// Box B -> pindah isi ke box A
// Box C -> pindah isi ke box B

const dewi = {
    name: "Dewi",
    kalkukasi: (a: number, b: number) => {
        return a + b
    }
}

dewi.kalkukasi(1, 2)
